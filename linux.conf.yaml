- defaults:
    link:
        relink: true
        # Uncomment to force linking.
        #force: true

- link:
    ~/.linux: linux/linux
    ~/.gitignore_global: linux/gitignore_global

- shell:
    -
      command: "echo install apps? [y/N] && \
                read var && \
                if [ $var = 'y' ];then ~/.dotfiles/linux/.scripts/apps_installer;fi"
      stdin: true
      stdout: true
